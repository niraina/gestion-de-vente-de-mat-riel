{% extends 'base.html.twig' %}

{% block title %}Modif Matériel sorti{% endblock %}

{% block body %}
    <div class="container">
        <h3 class="text-center mt-2">Modification d'un matériel sorti</h3>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-primary">Modifier</button>
            <a href="{{ path('materiel_sortie_index') }}" class="btn btn-warning">Retourner</a>
        {{ form_end(form) }}
    </div>
{% endblock %}
