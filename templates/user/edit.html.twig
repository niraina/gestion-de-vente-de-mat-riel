{% extends 'base.html.twig' %}

{% block title %}Modif role{% endblock %}

{% block body %}
    <div class="container">
        <h3 class="text-center mt-2">Attribution d'un rôle</h3>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-success">Modifier</button>
            <a href="{{ path('user_index') }}" class="btn btn-warning">Retourner</a>
        {{ form_end(form) }}
    </div>
{% endblock %}
