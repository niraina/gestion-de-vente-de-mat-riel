{% extends 'base.html.twig' %}

{% block title %}Modif Matériel{% endblock %}

{% block body %}
    <div class="container">
        <h3 class="text-center mt-2">Modification détail d'un matériel</h3>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-success">Modifier</button>
            <a href="{{ path('materiel_index') }}" class="btn btn-warning">Retourner</a>
        {{ form_end(form) }}
    </div>
{% endblock %}
