{% extends 'base.html.twig' %}

{% block title %}Nouveau Matériel entrée{% endblock %}

{% block body %}
    <div class="container">
        <h3 class="text-center mt-2">Enregistrement d'un matériel entrée</h3>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-primary">Enregistrer</button>
            <a href="{{ path('materiel_entree_index') }}" class="btn btn-warning">Retourner</a>
        {{ form_end(form) }}
    </div>
{% endblock %}
