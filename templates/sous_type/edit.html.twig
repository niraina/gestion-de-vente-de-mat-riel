{% extends 'base.html.twig' %}

{% block title %}Modif type matériel{% endblock %}

{% block body %}
    <div class="container">
        <h3 class="text-center mt-2">Modification d'un type matériel</h3>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-success">Modifier</button>
            <a href="{{ path('sous_type_index') }}" class="btn btn-warning">Retourner</a>
        {{ form_end(form) }}
    </div>
{% endblock %}



